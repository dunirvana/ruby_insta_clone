<div id="user-actions" class="modal">
  <div class="user-action">
    <%= link_to "Editar seu perfil", edit_profile_path %>
  </div>

  <div class="user-action">
    <%# < %= link_to "Deslogar", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Deslogar da sua conta?' } % > %>
    <%# < %= link_to "Deslogar", destroy_user_session_path, method: :delete, data: { confirm: "Deslogar da sua conta?" } % > %>
    <%# < %= link_to "Logout", destroy_user_session_path, :method => :delete % > %>
    <%= button_to "Deslogar", destroy_user_session_path, method: :delete, class: 'button-as-link'  %>
  </div>
</div>